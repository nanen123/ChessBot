# Unity project context

- Inspected 2026-09-14: Unity 6000.3.10f1, URP 17.3.0, Input System 1.18.0 (new input only), Test Framework 1.6.0, ML-Agents 4.1.0 installed.
- Root: C:/UnityProject/ChessBot. Architecture authority: 001.Architecture.md. Existing modified files belong to the user and must be preserved.
- Starting scene: Assets/Scenes/SampleScene.unity. Initial inspection found no game C# or assemblies. Sprites are imported as multiple sprites; preserve their GUIDs and slicing.
- Implement Core without Unity dependencies; Application owns game state; Presentation submits commands; Bootstrap composes local play. Namespaces ChessBot.Chess.*, private fields _camelCase.
- User clarified that current scope is direct on-screen local play, not networking or Agent training.
- Unity MCP is exposed but reported zero connected instances. A local Editor has this project open. Validation must use available local compiler or Editor evidence without claiming an unperformed Editor test.
- Evidence: README.md, Codex/001.Architecture.md, Packages/manifest.json, ProjectSettings/ProjectVersion.txt, ProjectSettings/EditorBuildSettings.asset, Assets/Sprites/*.meta.

## Implemented local play
- Core, Application, Presentation, Bootstrap and separate EditMode/PlayMode test assemblies now exist.
- SampleScene creates LocalChessBootstrap after initial scene load. Resources/ChessArt.asset references existing sprite sub-assets and is generated by the Editor setup menu when absent.
- Core/input tests: 45 passed using the local Unity C# compiler and Mono fallback. Unity Editor compilation succeeded; isolated Unity EditMode 45/45 and PlayMode 1/1 passed.
- Known limitation: conservative dead-position cases only; arbitrary blocked positions remain unresolved. No network or Agent mode implemented.

